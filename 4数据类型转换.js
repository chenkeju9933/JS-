/* 
 	  转为字符串
				toString()
					var num = 10;
					var str = num.toString()
					
				String() 强制转换
					console.log(String(num))
					
				和字符串拼接的 都会变成字符串
		
	  转为数字型
				parseInt()
					let age = '18.1'
					let num = parseInt(age) //  num = 18   得到的是整数
					parseInt(rem120)   // 输出 NaN
					parseInt(120px)   // 输出 120
					
				parseFloat()     可以取小数
				
				Number()  强制转换
				
				利用 - * /   减 乘 除  可以隐示转换
				
	  转换为布尔型		
				Boolean()   '' 0 undefined null NaN  5个都是false  其余全部为true
				代表空的 否定的值 都是为false
 */