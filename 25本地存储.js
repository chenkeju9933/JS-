/* 
        sessionStorage.setItem(key,value)   储存数据
        sessionStorage.getItem(key)         获取数据
        sessionStorage.getItem(key)         移除数据
        sessionStorage/clear()              清除所有数据
                            生命周期为页面关闭的时候

        localStorage.setItem(key,value)     同上 但是生命周期是永久 除非手动删除
*/