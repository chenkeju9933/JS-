/* 
                switch (条件表达式判断) {
                    case value1匹配表达式:
                        执行代码1;
                        break;
                    case value2:
                        执行代码2
                        break;
                    default:
                        执行语句
                }

    switch  对比 if else 效率高(直接跳到匹配值得语句执行) 书写代码多 判断条件适合特定值 不能是区间
            case值 必须为常量
            case值 和 变量表达式 必须全等匹配
*/